{
  "name": "mspr",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun scripts/dev.mjs",
    "start": "bun scripts/start.mjs",
    "build": "bun --filter \"*\" build",
    "dev:server": "bun --filter @mspr/server dev",
    "dev:client": "bun --filter @mspr/client dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
